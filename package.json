{
  "name": "human-attack-bot",
  "version": "0.1.0",
  "description": "Reverse-engineering harness for HUMAN Security (formerly PerimeterX) bot detection: recon the first-party sensor, capture the collector payload, crack the XOR transport + base91 string table, and map the hashed signal names.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "postinstall": "npx playwright install chrome",
    "start": "node main.mjs",
    "main": "node main.mjs",
    "recon": "node recon.mjs",
    "mitm": "node mitm.mjs",
    "bypass": "node bypass.mjs",
    "cipher-probe": "node cipher-probe.mjs",
    "decrypt": "node decrypt.mjs",
    "decode-strings": "node decode-strings.mjs",
    "build-dictionary": "node build-dictionary.mjs",
    "scan-strings": "node scan-strings.mjs",
    "diff": "node diff.mjs"
  },
  "dependencies": {
    "playwright": "^1.57.0",
    "prompts": "^2.4.2"
  }
}
